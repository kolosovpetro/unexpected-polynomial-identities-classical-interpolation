name: Build and Deploy PDF

on:
  push:
    tags:
      - v*
  workflow_dispatch:

env:
  ACTIONS_RUNNER_DEBUG: false
  # REQUIRES ${{ secrets.GH_ACCESS_TOKEN }} defined in secrets

jobs:
  build-deploy-pdf:
    uses: kolosovpetro/github-latex-template/.github/workflows/build-and-deploy-pdf-template.yml@main
    secrets: inherit
    with:
      file_name: 'unexpected-polynomial-identities-classical-interpolation'
      enable_patch_version: true
      runs_on: 'ubuntu-latest'
      workflow_name: 'Build and Deploy PDF'
      build_assets_path: |
        src/unexpected-polynomial-identities-classical-interpolation.tex
        src/unexpected-polynomial-identities-classical-interpolation.bbl
        src/unexpected-polynomial-identities-classical-interpolation.bib
        src/sections
      working_directory_for_latex: 'src'
