name: Build PDF

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

env:
  ACTIONS_RUNNER_DEBUG: false

jobs:
  build-pdf:
    uses: kolosovpetro/github-latex-template/.github/workflows/build-pdf-template.yml@main
    with:
      file_name: 'surprising-polynomial-identities-classical-interpolation'
      enable_patch_version: true
      runs_on: 'ubuntu-latest'
      workflow_name: 'Build PDF'
      build_assets_path: |
        src/surprising-polynomial-identities-classical-interpolation.tex
        src/surprising-polynomial-identities-classical-interpolation.bbl
        src/surprising-polynomial-identities-classical-interpolation.bib
        src/sections
      working_directory_for_latex: 'src'
